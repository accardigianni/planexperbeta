'use client';  
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#283e50',
    },
    secondary:{
      main:'#324d62'
    }
  },
});

export default theme;