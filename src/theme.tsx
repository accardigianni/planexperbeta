import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
    //   light: '#757ce8',
      main: '#283e50',
    //   dark: '#002884',
    //   contrastText: '#fff',
    },
  },
});

export default theme;